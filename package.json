{
  "name": "ChatBot",
  "version": "0.7.1",
  "scripts": {
    "eslint": "eslint core commands utils",
    "test": "jest",
    "serverless": "serverless",
    "pre-commit": "npm run eslint && npm run test",
    "invoke-local": "serverless invoke local -f telegram-bot -p event.json",
    "logs": "serverless logs -f telegram-bot -t",
    "deploy-function": "serverless deploy function --function telegram-bot  --verbose",
    "invoke": "serverless invoke -f telegram-bot -p event.json"
  },
  "dependencies": {
    "lodash": "^4.5.0",
    "mongoose": "^4.7.0",
    "request": "^2.58.0",
    "request-promise": "^4.1.1",
    "xml2js": "^0.4.17"
  },
  "devDependencies": {
    "eslint": "^3.19.0",
    "eslint-plugin-jest": "^1.0.2",
    "eslint-plugin-node": "^3.0.5",
    "jest-cli": "^18.1.0",
    "pre-commit": "^1.2.2"
  },
  "pre-commit": [
    "eslint",
    "test"
  ],
  "jest": {
    "testEnvironment": "node"
  },
  "license": "MIT"
}
