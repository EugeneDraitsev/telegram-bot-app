{
  "name": "chat-bot",
  "version": "1.1.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/EugeneDraitsev/telegram-bot-app"
  },
  "author": "Eugene Draitsev <ddrrai@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "sls offline start --stage local",
    "build": "bun run prepare:sharp-layer && sls package",
    "tsc": "tsc --noEmit",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "audit": "bun audit",
    "biome": "biome check ./src ./tools",
    "biome:fix": "biome check ./src ./tools --fix --unsafe",
    "logs": "sls logs -f telegram-bot -t",
    "diagram": "bunx @mermaid-js/mermaid-cli@11 -i .github/diagram/architecture.mmd -o .github/architecture-sqs-light.svg -c .github/diagram/light.json -b transparent --iconPacks @iconify-json/logos @iconify-json/mdi @iconify-json/simple-icons && bunx @mermaid-js/mermaid-cli@11 -i .github/diagram/architecture.mmd -o .github/architecture-sqs-dark.svg -c .github/diagram/dark.json -b transparent --iconPacks @iconify-json/logos @iconify-json/mdi @iconify-json/simple-icons",
    "migrate:chat-configuration": "bun tools/migrate-chat-configuration.mts",
    "validate:deployment-config": "bun tools/validate-deployment-config.mts",
    "deploy": "bun run validate:deployment-config && bun run prepare:sharp-layer && sls deploy",
    "prepare:sharp-layer": "bun tools/prepare-sharp-layer.mts"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.6",
    "@types/aws-lambda": "^8.10.162",
    "@types/jest": "^30.0.0",
    "esbuild": "0.28.1",
    "serverless": "^4.40.0",
    "serverless-esbuild": "^1.57.2",
    "serverless-offline": "^14.7.4",
    "serverless-offline-sqs": "^9.0.1",
    "typescript": "^7.0.2"
  },
  "workspaces": [
    "src/*"
  ],
  "overrides": {
    "undici": "6.28.0",
    "uuid": "11.1.1"
  }
}
