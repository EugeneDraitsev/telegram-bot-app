service: telegram

provider:
  name: aws
  stage: prod
  region: eu-west-1
  runtime: nodejs:6

package:
  exclude:
    - .idea/**
    - .git/**
    - node.exe
    - yarn.lock

functions:
  telegram-bot:
    handler: index.handler
    events:
      - http:
          path: /
          method: post
      - http:
          path: /
          method: get
      - schedule:
                rate: cron(0 7,9,11,13,15,17 ? * MON-FRI *)
                enabled: true
                input:
                  message:
                      text: /c
                      chat:
                        id: -1001111472139.0
